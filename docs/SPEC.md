# 상세 기획 (SPEC) — multiagent-sns

> 상태: **상세기획 (제안)** · 작성 2026-08-10 · 그린필드(과거 자산 재사용 없음, 전부 신규 구현)
> 상위 기획: [`PLAN.md`](PLAN.md) · 데이터 모델: [`ERD.md`](ERD.md) · 다이어그램: [`diagrams/`](diagrams/)
>
> **범위 선언(thin spec)**: 경계 계약·데이터 모델·측정 계획·운영만 못박는다.
> 에이전트 내부 행동(주제 선택 방식·글쓰기 스타일)은 프롬프트/플레이북에 위임하고 스펙으로 고정하지 않는다.

## 1. 목표 (한 문장)

개발자 주제 콘텐츠를 **IG 피드 게시물·IG 릴스·YouTube 쇼츠**에 **딥에이전트(deepagents)** 가 자동으로 기획·제작·발행하고, 반응(지표)을 학습해 **자기 채널을 키우는** 멀티에이전트 자율 성장 엔진.

## 2. 기능 요구사항 (FR)

### 2.1 에이전트 코어 (FR-C)

| ID | 요구사항 | 수용기준 |
|---|---|---|
| FR-C1 | 코어 = **deepagents 프레임워크를 그대로 도입**. Orchestrator(main) + 서브에이전트(Topic/Content/Media/Publisher/Analyst/Growth) 구성 | deepagents로 한 사이클(기획→제작→발행→학습)이 관통 실행 |
| FR-C2 | 서브에이전트는 **툴 계약**을 통해서만 외부와 상호작용: `render_media`·`publish`·`poll_metrics`·`read_stats`·`write_playbook` | 각 툴은 타입 시그니처 고정, 에이전트가 임의 부작용 못 냄 |
| FR-C3 | LLM 판단 계층 = Claude(`claude-sonnet-5`). 모델·시계·transport는 **주입식**(테스트에서 가짜 모델 대체 가능) | 가짜 모델로 한 사이클 결정론 재현 테스트 green |
| FR-C4 | LLM 산출 착지점은 **3곳으로 열거**(`content_item.body`·`playbook.guidance`·`analysis_note.body`). 그 외 수치/제어는 코드만 | 착지점 외 LLM 기록 경로 부재 테스트 |
| FR-C5 | 사이클 계획·위임은 Orchestrator가 수행, 각 서브에이전트 호출을 `run_event`로 관측 | 사이클 로그로 에이전트 흐름 재구성 가능 |

### 2.2 콘텐츠 생성 (FR-G)

| ID | 요구사항 | 수용기준 |
|---|---|---|
| FR-G1 | Topic Agent가 개발자 주제 도메인에서 주제 발굴, `topic` 풀 + `topic_stats` 참조 | 주제가 `content_item.topic_id`로 추적 |
| FR-G2 | Content Agent가 주제 → 포맷별 산출물 생성: 피드=카드뉴스 문안, 릴스/쇼츠=영상 대본(장면·자막·나레이션) | format별 스키마 준수한 `media_spec` 생성 |
| FR-G3 | 콘텐츠는 발행 전 **결정론 렌더 스펙(`media_spec`)** 으로 확정 저장(LLM 재호출 없이 재현 가능) | 같은 content_item→같은 렌더 입력 |

### 2.3 미디어 렌더 (FR-M)

| ID | 요구사항 | 수용기준 |
|---|---|---|
| FR-M1 | 이미지 카드 = Pillow 결정론 렌더(`media_spec`→PNG) | 같은 spec→같은 checksum |
| FR-M2 | **영상 = 템플릿 코드 합성**: 자막+슬라이드+TTS를 ffmpeg/moviepy로 합성. 생성형 비디오 모델 미사용 | 같은 spec→같은(또는 안정적) 산출, 릴스/쇼츠 규격(세로 9:16, 길이 제한) 충족 |
| FR-M3 | 렌더 산출은 `media_asset`(kind·storage_url·checksum) 저장, 호스팅은 추상화 seam | 저장소 벤더 교체 1지점 |

### 2.4 발행 (FR-P) — 공식 API만

| ID | 요구사항 | 수용기준 |
|---|---|---|
| FR-P1 | **Instagram Graph API**로 피드 이미지 + 릴스 영상 발행(2단계: 컨테이너 생성→게시) | 실 계정에 실 post_id 반환 |
| FR-P2 | **YouTube Data API v3**로 쇼츠 영상 업로드(#Shorts, 세로 영상) | 실 채널에 실 video_id 반환 |
| FR-P3 | **멱등 발행 원장**(`publish_attempt` 상태머신): 재시작 시 이중 발행 0 | 크래시 3지점(pending/container/publish) 재시작 복구 테스트 |
| FR-P4 | 오류 분류: transient(재시도)/영구(토큰만료·한도) 구분, 미분류는 `error_raw` 원문 보존 | 한 채널 실패가 다른 채널 발행에 무영향(격리) |
| FR-P5 | **발행 한도 준수**: 채널별 API 쿼터/발행 한도를 스케줄러가 존중(초과 시 대기·스킵+알림). *현재 알려진 상한은 §7 참조, 구현 전 최신값 재확인* | 한도 근접 시 발행 지연·알림, 계정 차단 리스크 회피 |

### 2.5 반응 학습 (FR-L)

| ID | 요구사항 | 수용기준 |
|---|---|---|
| FR-L1 | Analyst Agent가 발행 후 복수 시점에 인사이트 폴링 → `metric_observation`+`metric_value`. **결측=NULL**(0 금지) | 미노출 지표가 NULL로 저장, CHECK 강제 |
| FR-L2 | `RewardFn(관측창)→float\|None`. goal별 산식, 데이터 부족/미확정=NULL(학습 제외) | reward NULL 행은 밴딧 집계에서 제외 |
| FR-L3 | `topic_stats`(주제×포맷×채널) 집계 → Growth Agent 밴딧이 다음 사이클 변형 선택. **선택은 루프 밖 결정론(시드 고정)** | 같은 시드·같은 통계→같은 선택 |
| FR-L4 | `playbook` 갱신(성과 요약·지침) → 다음 사이클 콘텐츠 생성에 참조 | 플레이북 버전 증가, 콘텐츠에 반영 |
| FR-L5 | **LLM 분석글(정직 귀인)**: 입력=DB 근거. 인과 주장은 근거 인용 가능할 때만, 불가하면 **"근거 부족" 명시**. 수치는 코드 계산, LLM은 서술만. 출력 검증기가 인용 없는 단정·실재하지 않는 post_id를 거부 | 검증기 거부 테스트, 지어낸 인용 0 |

### 2.6 웹 대시보드 / 운영 (FR-W) — 선택(후속 가능)

| ID | 요구사항 | 수용기준 |
|---|---|---|
| FR-W1 | 채널 온보딩: IG 프로 계정/YouTube 채널 OAuth 연동, 토큰 **암호화 저장**, 검증 핑 | 검증 실패 시 연동 불가 |
| FR-W2 | 운영 설정: goal·발행 슬롯·주제 도메인·채널/포맷 on-off. 변경=새 국면(이력 보존·기저 재수집) | 변경 시 옛 데이터 불변 |
| FR-W3 | 성장 리포트: 추이 차트+기울기(자기 베이스라인 대비)+LLM 분석글(근거/뱃지) | 수치=코드, 분석글=근거 or "근거 부족" |
| FR-W4 | 모니터링·알림: 발행 실패·토큰 만료·한도 초과를 운영자에 통지 | 강제 실패 주입 시 알림 |

## 3. 비기능 요구사항 (NFR)

| ID | 요구사항 | 수용기준 |
|---|---|---|
| NFR-1 | **공식 API만 사용**(비공식 스크래핑/자동화 금지) — 계정 안전 | 의존성·코드에 비공식 클라이언트 0 |
| NFR-2 | **결정론 재현** = 테스트 타당성 전제. 주입식 모델·시계·transport, 시드 고정 | 가짜 모델 사이클 재현 CI 강제 |
| NFR-3 | **missing=NULL** DB CHECK 물리 강제 | CHECK 위반 삽입 거부 |
| NFR-4 | **멱등성**: 이중 발행 0, run_event append-only(best-effort) | 재시작 복구·이벤트 유실이 발행 성공 막지 않음 |
| NFR-5 | **자기 채널 성장만 측정**: 자기 베이스라인 대비 기울기. 타 계정과 절대 순위 비교 금지 | 비교 UI 부재, 한계 문구 상시 |
| NFR-6 | **정직 귀인**: 근거 없는 인과 주장 금지, 오버클레임 금지, n=1·초기 표본 한계 명시 | 리포트 한계 절 |
| NFR-7 | **시크릿 암호화**: 토큰 앱레벨 암호화 후 DB 저장, 키=env/Secret Manager, 키 회전(key_version) | 평문 grep 0, 키 부재 시 기동 실패 |
| NFR-8 | **인프라 상한 = Docker Compose**(단일 호스트), CI/CD=GitHub Actions. k8s 등 과설계 금지 | compose로 전 서비스 구동 |
| NFR-9 | **마이그레이션** 전진 순번·트랜잭션 원자성 | 적용·버전 갭 테스트 |
| NFR-10 | **계정 안전 운영**: 워밍업 후 연동, 보수적 발행 빈도, 한도 존중 | 최초 발행 전 체크리스트 |

## 4. 예외 · 엣지 · 테스트 케이스 (로직 체크)

| 요구사항 | 케이스 | 유형 | 기대 결과 |
|---|---|---|---|
| FR-P3 | 발행 중 크래시(pending/container_created/publish 3지점) | 예외 | 상태별 복구, 이중 발행 0 |
| FR-P4 | IG 토큰 만료(190류)/YouTube 401 | 예외 | 해당 채널만 중지+알림, 타 채널 발행 계속 |
| FR-P5 | YouTube 업로드 쿼터 소진 / IG 24h 발행 한도 초과 | 예외 | 잔여 발행 스킵·다음 창 대기+알림, 계정 리스크 회피 |
| FR-P1/P2 | 컨테이너 생성 성공 후 게시 실패(transient) | 엣지 | RetryPolicy 지수 백오프, container 재사용 |
| FR-M2 | 영상 규격 위반(길이 초과·가로 영상) | 예외 | 렌더 단계 사전 검증 실패 → 발행 차단 |
| FR-M1/M2 | 같은 spec 두 번 렌더 | 설계 | 같은 checksum(결정론), 산출 재사용 |
| FR-L1 | 플랫폼이 지표 미노출(신규 게시물·비공개 지표) | 엣지 | metric_value=NULL, 0으로 안 채움 |
| NFR-3 | missing=true인데 value 동시 삽입 | 예외 | CHECK 위반 거부(스키마 차단) |
| FR-L2 | 관측 창 표본 부족 / reward 산식 미확정 | 예외 | reward=NULL, 학습에서 제외 |
| FR-L3 | topic_stats 빈 데이터(콜드 스타트) | 엣지 | 균등 탐색(sweep-first)으로 시작 |
| FR-L3 | 같은 시드·같은 통계 재실행 | 설계 | 같은 선택(결정론) |
| FR-L5 | 분석글이 실재하지 않는 post_id 인용 | 예외 | 출력 검증기 거부·재생성, 미통과 시 미게시 |
| FR-L5 | 인용 불가한 외부요인 추정 주장 | 정상 | insufficient_evidence=1, "근거 부족" 표기 |
| FR-L5 | 분석글 수치가 코드 집계와 불일치 | 예외 | 검증기 거부(수치는 코드만) |
| FR-C3/NFR-2 | 실모델로 재시작 시 본문 재생성 | 설계 | `content_item.body` 정본 유지(재생성 금지, 비결정 방지) |
| FR-C4 | LLM이 착지점 외 테이블 기록 시도 | 설계 | 툴 미노출로 불가 — 부재 단정 테스트 |
| NFR-7 | 암호화 키 env 부재 / 키 회전 | 예외·정상 | 기동 실패 / key_version 병존 후 재암호화 |
| FR-W2 | 운영 중 goal/도메인 변경 | 정상 | 새 국면(기저 재수집), 옛 데이터 불변, 혼합 기울기 0 |
| FR-P5 | 동일 슬롯 다채널 발행 폭주 | 동시성 | 러너 큐 직렬화, 한도 내 분산 |

## 5. 성공 기준 (DoD)

1. **M1(실환경 관통)**: IG 피드 1건 + IG 릴스 1건 + YouTube 쇼츠 1건을 딥에이전트가 자동 발행 → 실 post_id/video_id 확보 → 지표 폴링 → reward 산출 → 리포트/분석글 열람까지 **한 번 관통**.
2. 밴딧 학습 루프가 `topic_stats`/`playbook`을 갱신하고 다음 사이클 선택에 반영됨(결정론 재현).
3. 결정론·멱등·결측=NULL·정직귀인 테스트가 CI(GitHub Actions)에서 green.
4. Docker Compose로 전 서비스가 단일 호스트에서 구동.

## 6. 미결정 (결정 필요)

| 항목 | 확정 시점 |
|---|---|
| goal 프리셋 정의(조회수/팔로워/저장/시청유지 중 우선순위)·reward 산식 | M1 실측 후 사전등록 |
| TTS 엔진 선택(클라우드 TTS vs 로컬) 및 저작권/음성 톤 | 미디어 모듈 착수 시 |
| 영상 템플릿 디자인 시스템(폰트·브랜드·장면 전환) | 미디어 모듈 착수 시 |
| 웹 대시보드 범위(MVP 필수 vs 후속) 및 스택 | 팀 협의 |
| 멀티 채널·멀티 계정(SaaS 다중테넌트) 확장 여부 | 이번 범위는 **단일 운영자/팀 자기 채널** 전제, 확장은 후속 |
| deepagents 세부 API(서브에이전트 정의·툴 등록 방식) | 코어 모듈 착수 시 프레임워크 문서로 확정 |
| PK 타입·ORM·미디어 저장소 | ERD §8 참조 |

## 7. 외부 제약 (구현 전 최신값 재확인)

- **Instagram Graph API**: 프로페셔널(비즈니스/크리에이터) 계정 + 연결된 Facebook 페이지 필요. 콘텐츠 게시 API는 2단계(미디어 컨테이너→게시). 계정당 24시간 발행 한도 존재(과거 25건 기준). 릴스는 동영상 컨테이너로 발행.
- **YouTube Data API v3**: `videos.insert` 업로드는 쿼터 비용이 큼(과거 기준 1회 ~1600 units, 일 기본 쿼터 10,000 units → 하루 수 건 규모). 쇼츠는 세로 영상 + #Shorts.
- **Meta/Google 심사**: 자기 계정 운영 범위를 넘어 타인 계정 대상 서비스로 확장 시 앱 검수(App Review) 게이트 발생 — 이번 범위(자기/팀 채널) 밖.
- 위 수치는 정책 변동이 잦으므로 **구현 착수 시 공식 문서로 재확인**한다.
