"""3단 레이아웃 가운데 칸(정사각 슬롯)의 **공통 규격** — 변·배경·테두리.

이 값들은 슬롯 자체의 규격이라 무엇으로 채우든 같다. 코드 이미지든 개념 그림이든 실사
사진이든 같은 크기·같은 배경 위에 같은 테두리로 놓여야 한 영상 안에서 튀지 않는다.

**왜 따로 있나.** 원래 [sns.render.code_image]에 있었다 — 코드 이미지가 이 슬롯의 첫
구현이었기 때문이다. 그러다 개념 그림·사진·spec이 전부 거기서 상수를 가져가면서, **개발
전용 모듈이 도메인 중립 모듈들의 의존성 뿌리**가 됐다. 코드를 쓰지 않는 도메인도
`pygments`를 물게 되고, 팩에서 `square_sources`의 code를 빼도 import 그래프는 그대로였다.

규율 한 줄: **공통은 중립 모듈에, 도메인 전용은 잎사귀에.**
[tests/test_render_layering.py]가 이 방향을 강제한다.
"""

# 3단 레이아웃의 정사각 변 (1080 - 좌우 여백 70×2).
DEFAULT_SIZE = 940

# 슬롯 바탕과 테두리 — 어두운 브랜드 팔레트. 위아래 검은 밴드와 대비가 나면서도
# 사진이 들어왔을 때 경계가 보이도록 테두리를 한 단계 밝게 둔다.
BACKGROUND = (13, 17, 23)
EDGE = (48, 58, 72)
