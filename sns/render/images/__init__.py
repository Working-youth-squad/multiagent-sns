"""주제 이미지 트랙 — 스톡 검색 → FR-Q7 게이트 → 정사각 변환 → 저장.

3단 레이아웃의 가운데 칸은 코드가 1순위다([sns.render.code_image]). 코드가 없는 컷은
그라데이션만 남아 화면이 비는데, 그 자리를 주제에 맞는 사진으로 채우는 게 이 패키지다.

**검색은 렌더 밖에서 한 번만 돈다.** 렌더 시점에 검색하면 같은 `media_spec`이 날마다
다른 영상을 낳아 FR-M1이 깨진다. 그래서 생성 시점에 `image_query`를 풀어
`image_ref`(콘텐츠 주소)로 못박고, 렌더러는 못박힌 바이트만 읽는다.
"""
